#!/bin/bash

# Do not edit this file unless you know what you are doing

banner() {
	echo ""
	echo "------------------------------------------------------"
	echo "-                                                    -"
	echo "-                 SuperR's Kitchen                   -"
	echo "-                     by SuperR                      -"
	echo "-                                                    -"
	echo "------------------------------------------------------"
	echo ""
}

cd $tools
( exec mkbootimg/mkboot $romdir/bootimg $romdir/boot.img >> $logs/boot.log )
banner
echo "boot.img packed, ramdisk remains"
echo ""
read -p "Press ENTER to return to the boot menu"
clear
cd $scripts
exec ./boot_tools
exit
