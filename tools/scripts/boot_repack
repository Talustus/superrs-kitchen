#!/bin/bash

# Do not edit this file unless you know what you are doing

banner() {
	echo ""
	echo "------------------------------------------------------"
	echo -e "-\e[1;107;30m                 SuperR's Kitchen                   \e[0m-"
	echo -e "-\e[3;107;30m                     by SuperR                      \e[0m-"
	echo "------------------------------------------------------"
	echo ""
}

cd $tools/boot
( exec mkbootimg/mkboot $romdir/bootimg $romdir/boot.img >> $logs/boot.log )
rm -rf $romdir/bootimg
clear
banner
echo "boot.img packed, ramdisk deleted"
echo ""
read -p "Press ENTER to return to the boot menu"
cd $scripts
exec ./boot_tools
exit
